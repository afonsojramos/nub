# Known-red nub MUTATION scenarios: "<fixture> <pm> <reason>".
# Same discipline as tests/conformance/expected-failures.txt — this list must
# SHRINK. A listed scenario that passes fails the run (XPASS-STALE) so the green
# flip is recorded by deleting the entry in the same commit as the fix.
#
# A red here is a nub WRITE-PATH divergence the differential caught: nub's
# mutated lockfile/manifest does not match what the real PM produces for the
# SAME mutation (add/remove/update). Each entry names the exact divergence
# richly enough to open a fix thread.

